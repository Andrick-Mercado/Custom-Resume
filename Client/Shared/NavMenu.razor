@using CustomResumeBlazor.Domain
<MudNavMenu>
    <MudNavLink Href="" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.AccountCircle">About</MudNavLink>

    @if (hasLoaded)
    {
        // parse sorted pages
        foreach (var pageDetails in sortedOtherPages)
        {
            <MudNavLink Href="@pageDetails.Endpoint" Match="NavLinkMatch.All" Icon="@IconMapper.GetIcon(pageDetails.Icon)">@pageDetails.Title</MudNavLink>
        }
    }
    else
    {
        @foreach (var _ in Enumerable.Range(0, 3))
        {
            <MudNavLink Icon="@Icons.Material.Filled.IncompleteCircle">Loading...</MudNavLink>
        }
    }

</MudNavMenu>
